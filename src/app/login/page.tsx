import Login from "../login";

export const metadata = {
  title: "Login | Portal Access C-137",
  description: "Accede al portal interdimensional",
};

export default function LoginPage() {
  return <Login />;
}
